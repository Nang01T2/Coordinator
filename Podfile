use_frameworks!

target 'Coordinator-iOS' do
  platform :ios, '10.0'
  podspec :path => 'Coordinator.podspec'
end

target 'Coordinator-macOS' do
  platform :osx, '10.10'
  podspec :path => 'Coordinator.podspec'
end

target 'Coordinator-tvOS' do
  platform :tvos, '10.0'
  podspec :path => 'Coordinator.podspec'
end

target 'Coordinator-watchOS' do
  platform :watchos, '3.0'
  podspec :path => 'Coordinator.podspec'
end

target 'Example' do
  platform :ios, '10.0'
  
  podspec :path => 'Coordinator.podspec'
end
